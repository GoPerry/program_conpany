#include<stdio.h>#include<string.h>#include<stdlib.h>#include <malloc.h>typedef struct ST_Jpeg_Data{        char    Buff[4];        char    index;        long    songIndex;        long    checkIndex;        }t_t;t_t aa=// t_t *pp[3]={0};int main(){	  t_t *pp=(t_t *)malloc(sizeof(t_t));	  	 strcpy(pp[0]->Buff,"ab");		pp[0]->index='b';        // t_t a[10]={0};        // t_t* t=NULL;        // t=a;        // strcpy(t->Buff,"ab");        // t->index='b';        pp[0]->songIndex=1;        pp[0]->checkIndex=2;        // t++;        // strcpy(t->Buff,"cd");        // t->index='d';        // t->songIndex=3;        // t->checkIndex=4;        printf("%s\t %c\n",pp[0]->Buff,pp[0]->index);        // printf("%s\t%c\n",a[1].Buff,a[1].index);		free(pp);        return 0;}